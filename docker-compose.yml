version: "3.9"

services:
    joblens-ai:
      build:
        context: .
        dockerfile: Dockerfile

      container_name: joblens-ai

      ports: 
        - "8000:8000"

      env_file:
      - .env

      restart: unless-stopped
